services:
  - type: web
    name: backend-mysgym
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /api
    autoDeploy: true
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true